1) Use Splitter to split up a large blob of names from raw text and name data into just the names in JSON format - if you have a formatted JSON array of names, this step
won't be necessary
2) Use nameProducer to read in the associated JSON that splitter will create, and create another json that is an array of fake yahoo! email addresses
